import {lifeContext} from './life_context.js';
export const available=(s,cid)=>{const c=s.characters[cid],h=s.resources.households[c.current_household_id];return (h?.support_enabled?Math.max(0,h.household_resources)*s.config.support_fraction:0)+(s.resources.personal[cid]?.personal_inheritable_estate||0);};
export function spend(s,cid,amount){if(amount<0||!Number.isFinite(amount)||available(s,cid)+1e-8<amount)return false;const h=s.resources.households[s.characters[cid].current_household_id];const household=h.support_enabled?Math.min(amount,Math.max(0,h.household_resources)*s.config.support_fraction):0;h.household_resources-=household;h.one_time_expense+=household;s.resources.personal[cid].personal_inheritable_estate-=amount-household;return true;}
export function settleResources(s,workFractions){const changes=[];for(const h of Object.values(s.households)){const r=s.resources.households[h.household_id];const living=h.members.filter(id=>s.characters[id].alive);r.one_time_income=0;r.one_time_expense=0;r.recurring_income=living.reduce((v,id)=>v+(s.careers[id]?.status==='active'?s.config.wage*s.careers[id].wage*(workFractions[id]||0)*(lifeContext(s,id).away?s.config.away_income_multiplier:1):0),0);r.recurring_expenses=living.reduce((sum,id)=>sum+s.config.living_cost*(lifeContext(s,id).away?s.config.away_living_multiplier:1),0)+living.filter(id=>s.education[id]?.status==='active').reduce((sum,id)=>sum+s.config.education_cost*(s.education[id].funding_fraction??1),0);const old=r.household_resources;r.household_resources=Math.round((old+r.recurring_income-r.recurring_expenses)*100)/100;r.unfunded_months=r.household_resources<0?r.unfunded_months+1:0;changes.push({household_id:h.household_id,before:old,after:r.household_resources,income:r.recurring_income,expenses:r.recurring_expenses});}return changes;}
