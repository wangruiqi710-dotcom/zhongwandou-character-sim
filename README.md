# 种豌豆 · 人物模拟实验室（Static Mock）

这是人物模拟测试器的独立公开静态版本，只用于 Mock 规则测试。

- 完全在浏览器中运行，不依赖 Python 后端或真实 AI。
- 不包含 API Key、私有游戏设计文档或人物 JSON 存档。
- 数据只保存在当前浏览器的 `localStorage`，不同设备不会自动同步。
- 可以导出/导入完整测试存档，也可以分别导出测试日志和候选事件。

## 双世界测试

顶部选择 **现代** 或 **古代**。现代沿用原有 Mock 行为规则；古代采用不绑定朝代的前工业农业社会，身份、有限教育、路费、信息核实、家庭责任等会参与候选行为评分。页面一直显示 `STATIC MOCK MODE`。

- 两个世界分别保存人物、月份、月度日志、评价、候选事件、同情境对比和跨世界对比历史。
- 每边都支持随机人物 / 婴儿、推进1 / 3 / 12个月、5～20人同情境对比。
- “同人物跨世界”使用当前人物的完整快照和共同抽象情境，生成两种具体背景，展示候选行为、评分、选择和理由。两边共用同一组随机扰动，不执行成长或推进时间。
- 跨世界结果以一组独立历史存于发起世界，不写入另一个世界。每边结果都可评价或加入带世界标记的候选事件。
- 完整导出只包含**当前世界**，包括它发起的跨世界对比。需要迁移两边时，请分别导出两份。导入要求当前所选世界与存档一致；清空仅影响当前世界。
- 旧版浏览器存档自动归入现代，旧 key 保留原始副本；旧版导出也只能导入现代。

保存 key 为 `modern_pea_character_sim_static_v1` / `ancient_pea_character_sim_static_v1`。浏览器之间不共享数据。

## 临时规则 / TODO

世界规则集中在 `worlds.js` 的 `WORLD_CONFIGS`：允许 / 禁用背景、身份、事件方向、兴趣样例、约束权重和目标解释。人物属性量尺与技能成长公式共用。

- 古代事件来自有限的年龄 / 身份筛选模板；自由情境只作简单关键词分类，尚不理解任意自然语言。婚配、生育、疾病、征役、资产等只讨论情境，不更新对应系统状态。
- 出行、教育、身份引介、家庭责任、性别机会成本及其分值均为可替换的测试假设，不代表所有前工业社会。性别成本只用于部分外出 / 学习机会，不影响天赋、技能上限或禁止某个性别生成身份。
- 古代输入和生成结果会检查禁用词。词表可以阻止列出的时代错位内容，但不是完整的语义时代检测器；新发现的错位需要补充词表或模板规则。
- 跨世界不改原始属性：现代专有兴趣以保留强度的活动类比解释，例如技术 → 手工；未知兴趣暂按手工。无法沿用的身份按年龄解释为普通农户或现代学生 / 无业，不推断原人物拥有土地、师资、亲属关系等。
- 抽象发展机会暂对应现代适龄兴趣体验、古代远集邀约 / 作坊试学，用于检验约束差异，不是完整的人生机会分配模型。旧经历保留在相同输入快照中，跨世界不直接续写另一个时代的经历。
- 统一年月标尺从2026年开始，仅供月份和年龄计算，不是古代历史纪年。
- 目标加权随机、兴趣到技能的样例映射和技能增量公式仍为临时规则；没有正式职业、经济、婚姻或世界模拟系统。
- 人生目标只在事件或行为含有明确长期方向标签时修正行为分数：`unrelated = 0`、`indirect = 0.2`、`direct = 1.0`。每个候选行为保存 `action_tags`、相关性和实际修正；无明确标签默认无关。

## 自测

可选开发检查：安装有 Node.js 时执行 `node test-worlds.cjs`，无需安装依赖。不影响网页使用。
检查存档隔离、旧版迁移、导入/刷新、月份、12岁目标、固定属性、两种对比、评价、候选事件、时代错位拦截、异常输出回滚和静态资源路径。

## GitHub Pages

本仓库从 `main` branch 的根目录 `/` 发布，使用 GitHub Pages 的 **Deploy from a branch** 模式。

访问地址：

https://wangruiqi710-dotcom.github.io/zhongwandou-character-sim/

## 文件

```text
index.html
app.js
worlds.js
styles.css
README.md
test-worlds.cjs
.nojekyll
```

`index.html` 只通过相对路径加载 `./styles.css`、`./worlds.js` 和 `./app.js`。`test-worlds.cjs` 不由网页加载。
