# Mock 2.2.1-alpha：玩家决策与紧凑界面

基于 `be8c02ab25dc44ec2c70f9c3daa0b24ec275261c`。本轮只在公开测试仓库工作。
冻结设计 architecture_v014 的 SHA-256 复核仍为
`6654561CAF9BE64D01FD1118A8F84E84A693B0AAFAA80ACD7FAA62841DB82E76`；没有创建 v015。

## 原因与修复边界

- 旧页面把已经完成的人物候选显示成“可选回应”，与待玩家点击的按钮没有明确区分。
- 内容库正式婚配进入通用内容分支：先抽内容回应，再执行婚配反应，未接入拒绝后的玩家干预入口。现在家庭先选具体对象或暂缓、拒绝；点击推动后才调用既有婚配函数，并保留内容历史与长期主题写回。
- 5个涉及持续照护与家庭分工的中型模板只按规模标为人物自主。现在用同一归属配置生成模板标记、界面筛选及实际等待入口，避免三处不一致。
- 默认只读摘要的生成不写入人物或历史，不抽随机数。

## 全模板审计

104个模板逐项核验，23个在涉及玩家家庭时进入玩家等待，81个保持自主；NPC自己的家庭仍自主。
原18个重大模板保留，以下5个的 `player_involvement` 与 `event_type` 改为家庭玩家决策：

| ID | 事件 |
|---|---|
| duty-2 | 加做一批活会撞上照护时段 |
| couple-2 | 配偶认为家中的责任分配不公 |
| care-2 | 连续照护让你错过了一次接活 |
| care-4 | 家人愿意轮流承担今后的照护 |
| network-4 | 邻里请你长期协助协调杂务 |

婚配、教育支持、重大职业/离乡、家庭资源、重大健康、责任分配、强制干预、继承、接班沿用或接入真实等待入口。外部向玩家家庭议亲也先等待玩家。
`src/config/player_event_routes.js` 集中定义归属；未改变事件进入候选池的权重或触发条件。

## 交互语义

- 人物自主：摘要标明人物最终决定；展开后显示人物考虑过的选项；概率在“为什么人物这样选择”中，不能点击成玩家选项。
- 玩家：置顶提示与决策弹窗，选项纵向排列。未点击前不抽取人物反应、不执行该安排、不生成最终结果。关闭弹窗只代表稍后决定，推进仍锁定。
- `player_choice`：实际点击的家庭安排；`character_choice`：后续人物反应，无额外反应时为 null；`final_outcome`：实际安排结果。页面及人生经历分别展示。
- 婚配：玩家选对象/暂缓/拒绝 → 本人、对象、对方家庭分别回应 → 硬条件检查及既有结果。本人拒绝后可尊重、再劝说、搁置，或在已有硬条件与额度允许时使用强制。不会覆盖对方拒绝。
- 暂缓/拒绝当前提议会实际暂停/结束对应议亲记录；保留历史和今后自主性。不会对未推动的提议进行人物抽样。
- 自动/批量运行在发现待玩家事件的当月立即退出，后续月份不运行。生产代码没有测试策略自动代选。

## 紧凑显示

- 导航：手机单行横向滑动，模拟、测试中心、事件库优先。
- 人物：姓名/年龄/身份、婚姻/子女/地点、健康/压力/资源三行摘要；NPC先显示同类摘要，再展开完整档案。
- 时间线：当前年展开，旧年折叠；普通月份单行或两行摘要，连续普通月份合并，内部保留逐月原始记录。
- 重要事件：时间、人物、事件、结果、最多三项变化；完成的玩家决定也自动折叠。重要NPC事件使用该人物的名字和事件，避免被主控人物日常标题遮盖。
- 长期故事：按已有持续问题/长期安排记录聚合，展开可看实际发展历史；不编造新剧情。
- 反馈：“评价”展开 ✓合理、⚠问题、★很好；保留现场、重放和高级JSON。
- 底栏：+1月、+3月、+1年、自动、暂停；运行时只留暂停，等待玩家时隐藏推进栏。
- 设置：紧凑/舒适；手机默认紧凑。偏好单独保存在浏览器，不修改模拟存档或反馈。

## 验证

新增 `tests/player-ui.cjs`：

- TEST-PLAYER-UI-01：等待期间人物/资源/随机状态未写回；明确输入后执行，可按命令精确重放。
- TEST-AUTONOMOUS-UI-01：自主事件没有玩家按钮，没有“可选回应”，清楚标明最终决定。
- TEST-MARRIAGE-PLAYER-01：普通与内容模板的外部议亲先由玩家决定，保留三方反应和内容后续历史，只有一次婚配处理。
- TEST-PLAYER-NO-AUTO-01：重复推进请求不会跳过等待，现代/古代均验证。
- 全104模板归属审计、暂缓/拒绝实际状态、受保护模块对比。

新增 `tests/compact-mobile.cjs`：真实生成66个月、75条记录、10个人物的合成存档，明确输入9次测试玩家决定；加载保存后在页面操作，并检查真实自动运行按钮和婚配按钮。

| 宽度 | 导航高度 | 人物摘要高度 | 最近3节点总高度 | 横向溢出 | 折叠旧年份 |
|---|---:|---:|---:|---|---:|
| 375px | 52px | 125px | 279px | 无 | 5 |
| 390px | 52px | 125px | 279px | 无 | 5 |
| 430px | 52px | 125px | 279px | 无 | 5 |

按钮触控高度至少44px；婚配选项48px；底栏不挡待决策选项。详情、反馈、人物跳转、显示偏好持久化均通过，无页面错误。

既有回归：`tests/v2.cjs` 96项，`tests/closure.cjs` 28项；`tests/mobile.cjs`、`tests/content-mobile.cjs` 三个宽度通过。旧测试只调整了展开路径与玩家模板数量预期。

源码对比确认：104个模板除决策归属外结构完全相同；概率、随机数、世界卡、长期状态、持续问题、重放、反馈持久化源码未变化。遗传及人物属性定义未修改。模拟改动仅为玩家等待、家庭直接安排与婚配拒绝后的交互修复。

## 当前边界

- 旧版运行保持只读、可导出；不把旧结果静默迁移或重算成2.2.1。
- 宽度验收使用 Windows Edge 浏览器自动化及截图检查，不等同于已在真实 iPhone/Android 设备验证。
- 仅压缩展示，完整历史和反馈仍保留，大量长期运行仍可能占用较多浏览器存储。
- 年初当前年记录少时，最近三到五件跨年经历需要展开上一年；旧年默认折叠遵循本轮要求。
- 部署提交和 Pages 实际构建状态在交付消息中记录，本文不提前声称构建成功。
